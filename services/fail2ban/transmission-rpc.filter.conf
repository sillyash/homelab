# 401s in nginx's shared access.log are effectively Transmission-RPC-only
# among this box's vhosts (Jellyfin/dropservice don't return HTTP 401).
[Definition]
failregex = ^<HOST> -.*"(GET|POST) \S+ HTTP/\d\.\d" 401
ignoreregex =
